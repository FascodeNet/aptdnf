#include "main.hpp"
using namespace nonsugar;
int main(int argc,char* argv[]){
    FascodeUtil::msg msgkun=FascodeUtil::msg();
    try{
        auto const cmd = command<char>(""
    }

    return 0;
}